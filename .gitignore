__pycache__/
*.pyc
.pytest_cache/
# No trailing slash: a trailing slash matches only directories, and a
# symlinked node_modules (as in a worktree) would get committed.
node_modules
extension/dist/
extension/python/
*.vsix
sdkgen/.cache/
.cache/
# venv created by ownbox.sh / manual `python -m venv .venv` installs
.venv/
*.egg-info/

# The team's real OpMode and hub configuration, used by test_sim.py's
# acceptance run; kept out of the public repo.
python/tests/fixtures/sim/fgc_main.py
python/tests/fixtures/sim/fgc_starter.txt
